Options -MultiViews
RewriteEngine On

# API → index.php de l'API
RewriteCond %{REQUEST_URI} ^/api/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^api/(.*)$ /api/index.php [L,QSA]

# Racine → page d'accueil jury
RewriteRule ^$ /index.php [L]
